import styled from "styled-components";
import Checkbox from "./checkbox-container.native";

const CheckboxContainer = styled(Checkbox)`
  width: fit-content;
  margin: 0 auto;
`;

export default CheckboxContainer;
