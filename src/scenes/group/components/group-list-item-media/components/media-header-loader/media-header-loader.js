import styled from "styled-components";
import Loader from "../../../../../../components/loader";

const MediaHeaderLoader = styled(Loader)`
  flex: 0;
  align-items: flex-start;
`;

export default MediaHeaderLoader;
