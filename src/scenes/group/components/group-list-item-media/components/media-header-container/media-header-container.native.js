import styled from "styled-components";
import Container from "../../../../../../components/container";

const MediaHeaderContainer = styled(Container)`
  display: flex;
  flex-flow: row;
  position: absolute;
  top: -30px;
  left: 15px;
`;

export default MediaHeaderContainer;
