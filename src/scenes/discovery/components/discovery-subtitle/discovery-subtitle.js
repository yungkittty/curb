import styled from "styled-components";
import Text from "../../../../components/text";

const DiscoverySubtitle = styled(Text)`
  margin-bottom: 120px;
  color: ${props => props.theme.secondaryColor};
`;

export default DiscoverySubtitle;
