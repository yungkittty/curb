import styled from "styled-components";
import ContainerScroll from "../../../../components/container-scroll";

const DiscoveryContainer = styled(ContainerScroll)``;

export default DiscoveryContainer;
