import React from "react";
import ListSection from "../../components/list-section";

const Discovery = () => (
  <ListSection
    section={[{}, {}]}
    renderItem={() => null}
    renderSection={() => null}
  />
);

export default Discovery;
