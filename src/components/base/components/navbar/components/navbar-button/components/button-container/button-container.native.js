import styled from "styled-components";
import Button from "./button-container.js";

const ButtonContainer = styled(Button)`
  width: 48px;
  height: 48px;
  margin-bottom: 12px;
`;

export default ButtonContainer;
