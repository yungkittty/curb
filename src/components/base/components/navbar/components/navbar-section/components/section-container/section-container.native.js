import styled from "styled-components";
import Section from "./section-container.js";

const SectionContainer = styled(Section)`
  padding-top: 12px;
  width: 72px;
`;

export default SectionContainer;
