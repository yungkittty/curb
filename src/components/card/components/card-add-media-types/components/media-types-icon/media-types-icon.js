import styled from "styled-components";
import Container from "../../../../../container";

const MediaTypesIcon = styled(Container)`
  position: relative;
  margin: 0px 12px;
`;

export default MediaTypesIcon;
