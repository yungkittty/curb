import styled from "styled-components";
import Text from "../../../../../text";

const EventTitleText = styled(Text)`
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
`;

export default EventTitleText;
