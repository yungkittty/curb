import styled from "styled-components";
import Icon from "../../../../../icon";

const ItemIcon = styled(Icon).attrs(() => ({ size: "extra-small" }))`
  position: absolute;
  right: 16px;
`;

export default ItemIcon;
